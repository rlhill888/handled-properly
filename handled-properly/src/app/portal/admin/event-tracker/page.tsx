import PortalPlaceholder from "@/components/PortalPlaceholder";

export default function EventTrackerPage() {
  return (
    <PortalPlaceholder
      eyebrow="Admin"
      title="Event Tracker"
      description="Track the status of every event in progress once this view is built out."
    />
  );
}
