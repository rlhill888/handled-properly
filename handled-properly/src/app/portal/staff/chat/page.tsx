import PortalPlaceholder from "@/components/PortalPlaceholder";

export default function StaffChatPage() {
  return (
    <PortalPlaceholder
      eyebrow="Staff / Vendor"
      title="Chat"
      description="Message the team and admins once this view is built out."
    />
  );
}
