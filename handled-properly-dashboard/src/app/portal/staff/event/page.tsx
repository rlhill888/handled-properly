import PortalPlaceholder from "@/components/PortalPlaceholder";

export default function StaffEventPage() {
  return (
    <PortalPlaceholder
      eyebrow="Staff / Vendor"
      title="Event"
      description="Your assigned event details will show up here once this view is built out."
    />
  );
}
